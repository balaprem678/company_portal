interface Settings {
    mode: string;
    smtp_host: string;
    smtp_password: string;
    smtp_port: string
    smtp_username: string
}

export interface smtpSettings {
    
    mode: string;
    smtp_host: string;
    smtp_password: string;
    smtp_port: string
    smtp_username: string    
}